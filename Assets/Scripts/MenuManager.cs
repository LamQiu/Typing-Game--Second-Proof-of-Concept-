using UnityEngine;
using UnityEngine.UI;

public class MenuManager : MonoBehaviour
{
    private CanvasGroup mainMenuCanvasGroup;  // 后面可以通过调这个控制主菜单淡入淡出之类的事情。

    /*
    TODO:
    1. InputField那里做一个字符限制。
    2. 更换字体
    3. LOGO换矢量图
    4. LOGO的阴影
    5. vertical layout group还不能适配不同的分辨率，需要找个解决方案。
    */
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
